/**
 * Foundation placeholder page.
 * Application UI is intentionally not built in Phase 1 (project foundation).
 */
export default function Home() {
  return (
    <main className="flex min-h-full flex-1 flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-semibold tracking-tight text-zinc-900 dark:text-zinc-50">
        OpsMap
      </h1>
      <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
        Project foundation — application UI arrives in later phases.
      </p>
    </main>
  );
}
